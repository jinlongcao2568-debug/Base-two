请在 AX9 新仓中执行 TASK-002：

目标：
把 docs/contracts 机器可读台账接入 JSON Schema 校验与 pytest。

必须遵守：
1. 先读 docs/baseline/ 下两份文档。
2. 不得修改未授权目录。
3. 只修改 docs/contracts/、docs/contracts/schemas/、scripts/validate_contracts.py、tests/contracts/、pyproject.toml。
4. 不得重命名现有 YAML 文件。
5. 先输出计划、影响文件、测试清单，再开始改。
6. 完成后输出：改动摘要、风险、测试结果、未完成项。

本轮不做：
- 抓取器
- 规则引擎
- facts 聚合
- API
- migration
- 跨文件复杂语义推理
